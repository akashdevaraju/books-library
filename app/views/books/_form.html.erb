<%= simple_form_for @book, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.association :girl,label_method: :name, value_method: :id, include_blank: false, :as => :hidden%>
  <%= f.association :girl,label_method: :name, value_method: :id, include_blank: false, disabled: true, label: 'Book Being Given To'%>
  <%= f.input :name , label: 'Book Name'%>
  <%= f.input :author %>
  <%= f.input :description %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                girls_path, :class => 'btn' %>
  </div>
<% end %>
